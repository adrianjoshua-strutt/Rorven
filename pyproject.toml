[project]
name = "rorven"
version = "0.1.0"
description = "Durable infrastructure for autonomous work."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["src"]

[tool.ruff]
line-length = 100
target-version = "py312"

